1，数组有没有length()这个方法? String有没有length()这个方法？
答: 数组没有length()这个方法，有length的属性。
　　String有有length()这个方法。
例如:   String a ="abcdef";
        String[] b={"g","h","i","j"};
	System.out.println(a.length()); //6
	System.out.println(b.length);   //4
 
2.在Android中，怎么节省内存的使用，怎么主动回收内存？
  回收已经使用的资源,  
  合理的使用缓存 
合理设置变量的作用范围…  application 对象 
//未来的某一段时间执行  
System.gc();

3. Service和Activity在同一个线程吗？
  答:  默认情况同一线程 main主线程 ui线程 .
 ( service里面不能执行耗时的操作(网络请求,拷贝数据库,大文件 ),在子线程中执行 new Thread(){}.start();}

4、Collection 和 Collections的区别。  
Collections是针对集合类的一个帮助类，他提供一系列静态方法实现对各种集合的搜索、排序、线程安全化等操作。
Collections.min(list);取集合中最小元素
Collections.copy(list1,list2);复制集合中元素
Collections.reverse(list);反转
Collections.Shuffling(list);打乱集合中元素
Collections.sort(list);从小到大排序
Collection是集合类的上级接口，继承他的接口主要有Set 和List. Map没有继承Collection接口.


5、同步和异步有何异同，在什么情况下分别使用他们？举例说明。
如果数据将在线程间同步(数据共享)。例如正在写的数据以后可能被另一个线程读到，正在读的数据可能已经被另一个线程写过了，那么这些数据就是共享数据，共享数据必须进行同步存
取。当应用程序在对象上调用了一个需要花费很长时间来执行的方法，并且不希望让UI线程等待方法的返回时，就应该使用异步编程，在很多情况下采用异步途径往往更有效率。
 
6.字符 字节 位 ?
答:
字节:byte，位:bit，1字节=8位二进制    1KB=1024B(byte)
数据存储的最小单位是位，但计算机处理数据一般都是以字节为单位。

java使用unicode编码
byte 1字节 		8 位二进制	1111 1111 =10进制255	-128到127
char(中英文字符)2 字节	16位二进制
boolean 		1 位二进制 True或者false   0或1
short占2个字节  	16位二进制
int占4个字节    	32位二进制  (1111 1111 1111 1111 1111 1111 1111 1111) = 10进制42亿      int 的取值  -21亿  21亿    
long占8个字节   	64位二进制
float占4个字节		32位二进制
double占8个字节		64位二进制

一个英文字母、或一个汉字都是1个Unicode 代码单元，Unicode目前普遍采用的是UCS-2,它用两个字节来编码一个字符，一个字符在Unicode中就是一个代码单元。

别问我1汉字占几个字符?
utf-8编码英文字符所占字节数:1
utf-8编码中文字符所占字节数:3
utf-16编码英文字符所占字节数:4
utf-16编码中文字符所占字节数:4
GBK编码英文字符所占字节数:1
GBK编码中文字符所占字节数:2
ASCII编码英文字符所占字节数:1
ASCII编码中文字符所占字节数:1

1个16进制数  占4位2进制      例如16进制   2=0010  6=0110    26=00100110
1个8进制数   占3位2进制       例如8进制	2=010   6=110     26=010110

7.
Android中的长度单位详解?
答:
dp也叫dip。
dp，就是把屏幕的高分成480分，宽分成320分。比如你做一条160dip的横线，无论你在320还480的模拟器上，都是一半屏的长度。

px：表示屏幕实际的象素。例如，320*480 的屏幕在横向有320个象素，在纵向有480个象素。
TextView 的宽度设成160px，在密度为320 的3.2 寸屏幕里看要比在密度为160 的3.2 寸屏幕上看短了一半。

sp:scaled pixels — best for text size (放大像素)（与刻度无关的像素）：主要处理字体的大小，但是可以根据用户的字体大小等首选项进行缩放。

in：表示英寸，是屏幕的物理尺寸。每英寸等于2.54 厘米。例如，形容手机屏幕大小，经常说，3.2（英）寸、3.5（英）寸、4（英）寸就是指这个单位。这些尺寸是屏幕的对角线长度。
如果手机的屏幕是3.2 英寸，表示手机的屏幕（可视区域）对角线长度是3.2*2.54 = 8.128 厘米。

设置表示长度、高度等属性时可以使用dp或sp。
但如果设置字体，需要使用sp。
根据google的推荐，像素统一使用dp，字体统一使用sp   