1.通知：（系统服务，相当于与另一进程进行通信）
？：在无界面时，服务进程在运行，需要与用户进行交互
系统服务：
功能：
1）常驻，清除
2) 更新通知（只要id相同重新发送即可）
如要一直显示ticker,则要在发消息之前cancle消息（qq)
3）点击通知后，进入意图
由于通知是在系统程序中，不可在这直接加监听,涉及跨进程通信
builder1.setContentIntent(PendingIntent的对象);

4)设置进度条

5)自定义通知ui

6)实现自定义ui里的点击意图
