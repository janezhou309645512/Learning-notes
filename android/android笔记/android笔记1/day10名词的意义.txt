1.方法的重写：只要调用子类对象时，运行的是子类的方法
重写父类的方法时：调用super()
最好：不删，在在其后面写其他代码（特殊则在前写）
2.重载（overload)与重写（override)的区别：重写与重载都不需要在意返回值类型，只需看参数即可
package: 
import:声明类/引入类  包类.类名
private--不写(同包类间可以互相访问)--protected(同包以及子类）--public的区别
static(属于类，不属于实例）:用在初始化静态资源,只能类去访问，不需通过对象去访问
修饰的成员最先存入内存
1）静态变量(都调用一个地址）在多个实例间公用的数据
2）静态方法,(static不能直接访问非static成员）
*静态初始化块 
class A{
static{
类加载时，只执行一次｝｝
final:(只能赋值一次）
1）变量不能变
2）类不能继承
3）方法不能重写
常量：static final 不可变，只有一个内存地址，节省内存
常量名：大写下划线
对象创建过程：
先加载类的静态变量与方法，在加载类的非静态变量与方法
