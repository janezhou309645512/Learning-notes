1.跨进程间通信：
在两个应用程序间（包括系统与程序间）进行数据交互，有且只能激活
另一程序组件，且传递数据,不能直接改变数据
PendingIntent:

2.AIDL(实现跨进程启动service）
1)java中的接口去掉public，然后将扩展名修改为.aidl
2)aidl中的文件里放的数据类型（除short)，如果要放类，
则要用Parcelable序列化
案例：
跨进程间绑定service
步骤：
1）service的绑定
2）传入参数
3）传入指定的对象