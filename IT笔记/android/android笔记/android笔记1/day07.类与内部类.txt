1.为什么要引用类：对同一属性一组不同数据的封装，是一个数据类型
类：一类个体的统称（猫）----对象：单个个体（这只猫）
类：属性/特征---数据，行为----方法
成员变量：有默认值，自动初始化 数字为0，boolean false 引用类型 null
2.1.1).在一个文件中可以包含多个类
  2).只含有一个public 修饰一个类
  3).public修饰的类与文件名相同
3.内部类：内部类只能对外部类用 不能对外用，可以访问外部类的所有成员
内部类：
1）成员非静态内部类：
A a=new A();
A.Inner2 i2=a.new Inner2();
  
2）成员静态内部类：只可以访问外部类的静态成员
（静态的成员最先加载在内存中）
A.Inner1 i1=new A.Inner1();
 Class A{
static class Inner2{}
3)局部内部类：不可以加访问权限控制符
只能在局部使用，局部内部类中使用外部局部变量加final
//方法中的类：Weapon f1(){
class AK47 implements Weapon{
void kill(){}
AK47 a= new AK47();
return  a;}
4)匿名类：new{};相当于创建一个子类，只创建一次对象是使用
在方法中创建，具有局部内部类的特点
weapon f2(final String string){
Weapon w=new Weapon(){实现接口的的子类｝；}
内部类语法总结：